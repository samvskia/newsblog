require.config({
    deps: ["main"],
    baseUrl: "js",
    paths: {
        //Libraries
        jquery: "libraries/jquery-3.3.1.min",
        bootstrap: "libraries/bootstrap/bootstrap.min",

        //Directories
        views: "../views",
        templates: "../templates"
    }

});